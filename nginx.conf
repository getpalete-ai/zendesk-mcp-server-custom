events {
    worker_connections 1024;
}

http {
    upstream mcp_backend {
        # Session affinity - critical for maintaining SSE sessions
        # Routes same client IP to same backend server
        ip_hash;
        
        # Load balance across MCP instances on different ports
        server zendesk-mcp:8021;
        server zendesk-mcp:8022;
        server zendesk-mcp:8023;
    }

    server {
        listen 80;
        server_name localhost;

        # Health check endpoint
        location /health {
            access_log off;
            return 200 "healthy\n";
            add_header Content-Type text/plain;
        }

        # MCP SSE endpoint
        location /sse {
            proxy_pass http://mcp_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # SSE specific settings
            proxy_http_version 1.1;
            proxy_set_header Connection "";
            proxy_buffering off;
            proxy_cache off;
            proxy_read_timeout 24h;
            
            # Timeouts
            proxy_connect_timeout 30s;
            proxy_send_timeout 30s;
        }

        # Health check and status endpoints
        location / {
            proxy_pass http://mcp_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}